<h1>List of Restaurants</h1>

<% @restaurants.each do |r| %>
  <p><%= link_to r.name, restaurant_path(r) %> located at <%= r.address %> serving <%= r.category %> food</p>
<% end %>

<%= link_to "Add restaurant", new_restaurant_path,
 class: "btn btn-primary"%>
